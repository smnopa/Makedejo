---
import Layout from '../layouts/Layout.astro';
import ItemCard from "../components/ItemCard.astro";
import { excursiones } from '../data/excursiones';
import Coments from '../components/Coments.astro';
---

<Layout isHomePage={false} title="Excursiones | Makedejo">
    <section class="relative z-0 h-[72vh] mt-[var(--nav-height)]">
  <div class="relative w-full h-full">
    <!-- Imagen para todas las pantallas -->
    <img src="/img/images/Banner_Excursiones.png" alt="Banner Excursiones" class="object-cover w-full h-full" />

    <!-- Texto superpuesto -->
    <div class="absolute inset-0 flex justify-start">
      <!-- Tener en cuenta estos márgenes para el resto de la página -->
      <h1 class="font-bold text-white text-3xl mt-11 ml-8 sm:text-4xl lg:text-4xl xl:text-5xl xl:ml-52 xl:mt-20">
        <span class="block xl:hidden">
          "Con Makedejo, cada paso es una nueva aventura."
        </span>
        <span class="hidden xl:block">
          “Con Makedejo, cada paso es una<br>nueva aventura.”
        </span>
      </h1>
    </div>
  </div>
</section>

  <section class="py-16 px-8 text-center">
		<div class="mb-8">
			<h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#0A3A4E]">
				Todos los planes y excursiones
			</h1>
		</div>
		<div class="max-w-fit xl mx-auto text-lg text-gray-500 leading-relaxed 2xl:px-40">
			<p>
				"Descubre la magia de cada rincón y deja que cada paso te lleve a nuevas aventuras. Con Makedejo, cada excursión se convierte en una historia que vale la pena contar."
			</p>
		</div>
	</section>

  <div class="max-w-6xl mx-auto my-12 px-4">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    {excursiones.map((excursion) => (
      <ItemCard
      id={excursion.id}
      nombre={excursion.nombre}
      detalles={excursion.detalles}
      img={excursion.img}
      tipo="excursiones"
      />
    ))}
  </div>
  </div>
  <!-- Seccion de Comentarios -->
  <section class="py-16 px-8 2xl:px-12">
	<div class="mb-20 text-center">
		<h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#0A3A4E]">
			Viajamos con Make!!
		</h1>
	</div>	
	<!-- Contenedor del Carrusel de Comentarios -->
	<div class="relative max-w-full xl:w-10/12 mx-auto carousel-container" data-card-width="320">
		<!-- Contenedor de los items del carrusel -->
		<div class="max-w-fit xl mx-auto text-lg text-gray-500 leading-relaxed 2xl:px-44 flex flex-col gap-5 sm:flex-row carousel">
			<Coments /><Coments /><Coments />
	</div>	  
	  <!-- Flechas de Navegación -->
	<button class="prev absolute top-1/2 left-0 transform -translate-y-1/2  bg-white p-3 rounded-lg shadow-lg text-gray-800 text-xl">
		&#10094;
	</button>
	<button class="next absolute top-1/2 right-0 transform -translate-y-1/2 bg-white p-3 rounded-lg shadow-lg text-gray-800 text-xl">
	  &#10095;
	</button>
   </div>
  </section>

</Layout>